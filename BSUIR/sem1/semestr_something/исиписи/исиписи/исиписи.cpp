
#include <iostream>
#include <vector>
int main()
{
    int n, m;
    std::cin >> n >> m;
    std::vector <int> s(n);
    for (int i = 0; i < n; i++) {
        std::cin >> s[i];
    }
    std::cout << "Hello World!\n";
}
