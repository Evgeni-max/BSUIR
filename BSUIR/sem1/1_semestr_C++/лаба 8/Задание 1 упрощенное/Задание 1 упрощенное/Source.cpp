#include <string>
#include <cstdio>
#include "..\CheckInput\chek.h"
#include "..\functions\functions.h"

product* a;

int main() {
    a = (product*)calloc(1, sizeof(product));

    long long sizeOfArray = 0;
   
	ok_lets_go(&sizeOfArray);
}

